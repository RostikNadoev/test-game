server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Standalone frontend image serves static Mini App assets only.
    # Public API/WebSocket traffic must go through the root nginx service in docker-compose.

    location / {
        try_files $uri $uri/ /index.html;
    }
}
